# 사천성 배틀 기획서

> 최종 업데이트: 2026-07-25 — 아이템 연속 사용·효과 공정성·HUD 재배치

## 보드 생성 규칙

- 12×8 전체 좌표에 face 1~24를 각각 4장씩 시드 기반으로 분산 배치한다.
- 기존의 가로 인접 도미노 48쌍을 미리 만드는 방식은 사용하지 않는다.
- 외곽에 노출된 좌표 쌍을 결정적 PRNG로 선택해, 저장된 48단계 해답이 실제 최대 2회 꺾임·외곽 경로 규칙으로 항상 완주되게 한다.
- 초기 보드와 자동 교착 셔플은 동일한 생성기를 사용하며, 셔플은 남은 face 다중집합·타일 ID·좌표·제거 상태를 보존한다.
- `solution`은 서버 내부 검증용이며 WebSocket snapshot에는 노출하지 않는다.

## 프로젝트 개요

미니게임 천국에 통합된 LAN 1:1 사천성 아이템 대전이다. 사람 또는 서버가 인증한 AI와 동일한 12×8 초기 배치의 독립 보드에서 3분 동안 짝을 제거하며, 높은 빈도로 얻는 6종 아이템을 연속 사용해 경쟁한다.

## 기술 스택

| 항목 | 기술 |
|---|---|
| 서버 | Node.js, 바닐라 JavaScript ES modules |
| 통신 | HTTP, WebSocket (`ws`, noServer 통합) |
| 클라이언트 | HTML, CSS, 바닐라 JavaScript |
| 에셋 | GPT Image 2 기반 래스터, SVG 문양 합성, WebP 배경 |
| 테스트 | Node test runner, Playwright |

## 아키텍처

### 디렉토리 구조

```text
sichuan-battle/
  assets/       # 24종 타일과 배경, 생성 원본·검증표
  lib/          # 규칙, 경로, solver, 아이템, 경기 상태
  public/       # 대기·플레이·결과 UI와 ko/en 클라이언트
  scripts/      # 에셋 재생성·검증
  tests/        # 단위·프로토콜·2브라우저·시각 QA
  bot.js        # 인증된 비브라우저 AI 참가자와 행동 예약 수명주기
  server.js     # 정적 파일과 서버 권위 WebSocket 앱
```

### 핵심 모듈

| 모듈 | 파일 | 역할 |
|---|---|---|
| 경기 상태 | `lib/game.js` | 플레이어별 보드·효과·타이머·결과·자동 셔플 관리 |
| 보드/해결기 | `lib/board.js`, `lib/solver.js` | 동일 시드 초기 배치와 완주 가능한 재배치 생성 |
| 경로 판정 | `lib/pathfinder.js` | 직선·1회·2회 꺾임과 외곽 연결 검증 |
| 아이템 | `lib/items.js` | 6종 드롭, 결정적 무작위 공격 대상과 대응 규칙 |
| AI 판단/실행 | `lib/ai.js`, `bot.js` | 합법 짝 탐색, 보통 난이도 템포, 상황별 아이템 사용과 예약 정리 |
| 서버 앱 | `server.js` | 런처 통합, 양쪽 공개 보드를 포함한 개인화 snapshot, 권위 검증, 복구·재대결 |
| 보드 UI | `public/js/board-view.js` | tileId keyed DOM을 유지하는 내 보드와 읽기 전용 상대 축소 보드, 선택·판정 피드백 렌더링 |
| 아이템 UI | `public/js/items-view.js` | 3슬롯, ko/en 설명, 효과 시간과 ARIA 렌더링 |

## 기능 목록

| 기능 | 설명 | 상태 |
|---|---|---|
| LAN 1:1 대전 | 양쪽 READY 후 같은 초기 배치의 독립 보드로 시작 | 완료 |
| 1인 AI 대전 | 런처 AI 채우기 또는 게임 내 CTA로 인증 AI와 즉시 시작 | 완료 |
| 서버 권위 AI | 일회성 토큰, 실제 공개 snapshot과 기존 프로토콜로 합법 행동만 수행 | 완료 |
| 독립 듀얼 보드 | 큰 내 보드와 실제 상대 상태의 읽기 전용 축소 보드를 PvP·AI에서 동시 표시 | 완료 |
| 타일 입력 피드백 | 선택 유지, 판정 대기, 성공 경로·제거, 사유별 실패와 후속 입력 복구 제공 | 완료 |
| 입력 신뢰성 | 250ms 동기화 중 포인터·터치·키보드 click 보존, 전송 성공 기반 pending과 2초 복구 제공 | 완료 |
| 사천성 경로 | 최대 2회 꺾임과 보드 바깥 1칸 경로를 서버가 판정 | 완료 |
| 해결 가능한 보드 | 24종×4장과 완전 해답을 생성하고 교착 시 경고 후 복구 | 완료 |
| 서버 권위 판정 | `matchId`, revision, 타일·아이템 소유권과 중복 요청 검증 | 완료 |
| 고빈도 아이템전 | 3슬롯의 잠금·뒤집기·안개·힌트·정화·방어막을 동기화 대기 없이 연속 사용 | 완료 |
| 힌트 | 본인 보드의 실제 연결 가능한 한 쌍만 3초 강조하고 상대에게 대상·경로를 숨김 | 완료 |
| 공정한 방해 대상 | 잠금·뒤집기·안개 대상을 유효 타일 전체에서 서버 결정적 난수로 분산 선택 | 완료 |
| 효과 수명주기 | 중첩 방해의 독립 만료와 정화·방어막을 처리하고 자동 교착 셔플 뒤 활성 효과를 복구 | 완료 |
| 반응형 경기 HUD | 데스크톱은 아이템·내 보드·상대 보드, 모바일은 내 보드·아이템·상대 보드 순서 | 완료 |
| 경기 결과 | 선완주, 시간 점수, 무승부와 양측 동의 재대결 지원 | 완료 |
| 연결 복구 | 세션 토큰으로 15초 안에 현재 경기 복구 | 완료 |
| 다국어·접근성 | ko/en, 키보드 1/2/3, ARIA, 저감 모션 지원 | 완료 |
| 커스텀 아트 | 고유 문양 타일 24종과 저대비 야간 정원 배경 적용 | 완료 |
| 런처 통합 | 전용 HTTP/WS 경로와 2인·AI 채우기 지원 카드 등록 | 완료 |

## 알려진 제약사항

- LAN 1:1 전용이며 AI 난이도는 보통 한 종류다. 3인 이상, 온라인 매치메이킹과 모바일 전용 UX는 지원하지 않는다.
- 인증, 인터넷 배포용 부정행위 방지, 영구 전적과 랭킹을 제공하지 않는다.
- 런타임은 생성된 PNG/WebP를 사용한다. 에셋 재생성에는 `scripts/build-assets.mjs`와 원본 파일이 필요하다.
- 390px 화면의 12열 내 타일은 약 28×30px로 일반적인 36px 이상 터치 권장치보다 작다.
- `/favicon.ico`는 별도 리소스가 없어 인앱 브라우저에서 404 메시지가 발생할 수 있으나 게임 동작에는 영향이 없다.

## 입력 및 pending 계약

- 타일 버튼은 `tileId`를 키로 경기 중 같은 DOM 객체를 재사용하며, 상태·locale·ARIA 속성만 제자리에서 갱신한다.
- 실제 `WebSocket.send()`가 성공한 짝 요청만 `requestId`, `matchId`, board revision을 가진 pending으로 전환한다.
- PAIR 응답은 현재 `requestId + matchId`가 모두 일치할 때만 처리하고, revision·경기·연결 변화에서는 pending과 선택을 정리한다.
- 응답이 2초 안에 오지 않으면 입력을 복구하며 늦게 도착한 응답은 상태를 바꾸지 않는다. 이후 권위 `STATE_SYNC`가 실제 보드 결과를 반영한다.

## 아이템 및 효과 계약

- 아이템 사용은 서버의 `matchId + slotId` 소유권과 `requestId` 중복 캐시로 승인한다. 서로 다른 슬롯은 이전 `inventoryRevision`을 보내도 각각 한 번 처리된다.
- 힌트는 합법 짝이 있을 때만 소비하며 본인 snapshot에 대상 두 타일만 공개한다. 상대 snapshot과 모든 공개 snapshot에는 힌트 경로를 넣지 않는다.
- 잠금·뒤집기·안개 대상은 경기 seed와 사용 순번에서 파생한 결정적 PRNG로 유효 후보 전체에서 비복원 선택한다.
- 방해 타일 플래그는 남은 활성 효과 전체에서 재계산한다. 자동 교착 셔플은 힌트만 정리하고 활성 방해 플래그를 복구한 뒤 완주 가능한 보드를 제공한다.

## 향후 계획

- 실제 두 기기 LAN 환경에서 아이템 빈도와 공격 지속 시간 체감 플레이테스트
- 모바일 터치성을 높일 경우 12열 전체 노출을 유지하면서 타일 간격 또는 선택 보조 방식을 별도 검토
- 필요 시 랭킹·매치메이킹과 AI 난이도 선택을 별도 범위로 검토
