# 윷놀이(Yutnori) 룰북 — 한국 표준 + 본 구현 비교

> **본 문서의 위상**
> 본 룰북은 **한국 전통 표준 윷놀이 룰**을 기준으로 한 lazyslimestudio `minigames/yutnori` 프로젝트의 권위 참조 문서이다.
> 모든 QA 시나리오와 향후 룰북 기반 테스트는 본 문서의 §번호를 인용하여 작성한다.
> matgo `docs/RULEBOOK.md` 와 janggi `docs/RULEBOOK.md` 와 동일한 13섹션 패턴을 따른다.
>
> **출처**
> - 한국어 위키백과 「윷놀이」 (https://ko.wikipedia.org/wiki/윷놀이, 1차 한국어 출처)
> - English Wikipedia 「Yunnori」 (https://en.wikipedia.org/wiki/Yunnori, 영문 교차 검증)
> - 한국민속대백과사전 (국립민속박물관) 윷놀이 항목 (전통 명칭 검증)
> - 나무위키 「윷놀이」 (한국어 보조 출처, 변형 룰 카탈로그용)
>
> **작성 원칙**
> 룰이 모호한 부분은 **"표준에 모호"**로 명시하고, 본 구현이 채택한 정책을 함께 기록한다. 자체 추측 금지.
> 본 구현의 모든 차이는 §13에 영향도(HIGH/MED/LOW) 라벨과 함께 정리되어 있다.

---

## 목차

- [§1 게임 개요](#1-게임-개요)
- [§2 말판(윷판) 구조 + 다이어그램](#2-말판윷판-구조--다이어그램)
- [§3 윷가락 결과 표](#3-윷가락-결과-표)
- [§4 말(馬) — 개수, 출발, 완주](#4-말馬--개수-출발-완주)
- [§5 이동 규칙 + 칸 수 표](#5-이동-규칙--칸-수-표)
- [§6 보너스(한 번 더) 던지기](#6-보너스한-번-더-던지기)
- [§7 잡기](#7-잡기)
- [§8 업기](#8-업기)
- [§9 백도(뒷도/빽도)](#9-백도뒷도빽도)
- [§10 분기점(모서리 + 중앙) 처리](#10-분기점모서리--중앙-처리)
- [§11 승리 조건](#11-승리-조건)
- [§12 QA 체크리스트](#12-qa-체크리스트)
- [§13 구현 노트 (구현 vs 표준 차이)](#13-구현-노트-구현-vs-표준-차이)

---

## §1 게임 개요

- **인원**: 본 구현은 **2인 1:1 PvP** 고정. 표준 룰은 2인 이상 팀전(짝수)이 가능하지만 본 구현은 LAN 1:1 단순화.
- **준비물(전통)**: 윷가락 4개(장작윷 또는 밤윷), 윷판(말판), 말 8개(각 편 4개).
- **목표**: 자기 편 말 4개를 모두 먼저 **완주("났다")** 시키면 승리.

> 출처 — 한국어 위키백과:
> > "윷놀이는 윷가락을 던지고 그 결과에 따라 말(馬)을 이동시켜 승부를 겨루는 전통놀이로, 나무로 윷을 만들어 여러 사람이 윷판 위를 돌아 다니는 표식자(말)로 구성된다. 윷판의 양쪽에 각각 말 4개씩을 놓고 윷을 교대로 던져 결승점을 4개 모두 먼저 통과시키는 편이 이긴다."

- **턴 구조**: 한쪽 편이 윷가락을 던져 결과를 얻고 → 결과대로 말을 옮긴다. 윷·모는 보너스 던지기 발생(§6). 보너스가 모두 소진되면 다음 편으로 차례가 넘어간다.
- **본 구현 채택**: `p1`(빨강) 선공 → `p2`(파랑) 후공. (표준은 양 편이 첫 던지기를 한 번씩 해서 더 큰 결과를 낸 편이 선공이지만, 본 구현은 선후공 결정 단계 생략 — §13-7 참조.)

(구현 위치: `server.js` `resetGame()` — `game.currentTurn = 'p1'`)

---

## §2 말판(윷판) 구조 + 다이어그램

### §2-1 전통 윷판 (표준)

- 외곽 사각형 둘레에 칸(자리) 20개, 그 안에 두 대각선이 교차하며 중앙(방)에서 만난다.
- 표준 명칭(한국민속대백과/나무위키 기준): 외곽 모서리 4개는 **참먹이(=출발/도착, 좌하)**, **모(좌상)**, **방(중앙)**, **찌(우상)**, **쨀(우하)** 등 다양한 지역 명칭이 있다. 본 구현은 명칭 대신 인덱스(좌표)로만 식별한다.
- 한국어 위키백과 정의:
  > "윷판으로 설명하면 가로 세로 다섯 개의 눈금의 정사각형과 그 안에 교차하는 두 대각선의 모양이며, 출발점은 정사각형의 한 꼭짓점이다."
- 외곽 각 변에는 4개의 일반 칸 + 양 끝 모서리 2개. 한 변에 칸 5개씩 × 4변 - 공유 모서리 4 = **외곽 20칸**.
- 두 대각선 위에 각각 4칸(중간 3 + 중앙 1 공유). 총 칸 수 = 외곽 20 + 대각 좌상~우하 3 + 대각 우상~좌하 3 + 중앙 1 공유 = **29칸**.

### §2-2 본 구현의 인덱스 매핑

본 구현은 표준 29칸을 다음 인덱스로 표현한다 (서버 `server.js` + 클라 `public/js/board.js` 동기).

| 영역 | 인덱스 | 설명 |
|------|--------|------|
| 외곽 좌하 모서리 | `0` | **출발점이자 도착점**. 표준 명칭 "참먹이". |
| 외곽 좌측 변 | `1, 2, 3, 4` | |
| 외곽 좌상 모서리 | `5` | 지름길A 분기점. 표준 명칭 "모(또는 모도)". |
| 외곽 상단 변 | `6, 7, 8, 9` | |
| 외곽 우상 모서리 | `10` | 지름길B 분기점. 표준 명칭 "찌". |
| 외곽 우측 변 | `11, 12, 13, 14` | |
| 외곽 우하 모서리 | `15` | 지름길A 합류 출구. 표준 명칭 "쨀". |
| 외곽 하단 변 | `16, 17, 18, 19` | `19` 다음 외곽 진행 = 완주. |
| 지름길A 중간 | `21, 22` | 좌상(5) → 중앙으로 향하는 두 칸. |
| 지름길B 중간 | `26, 27` | 우상(10) → 중앙으로 향하는 두 칸. |
| centerExitB 중간 | `24, 25` | 중앙(23) → 좌하(0) 출구로 향하는 두 칸 (전통 "날밭"). 2026-06-11 활성화. |
| 중앙(방) | `23` | 두 대각선이 만나는 칸. 분기점. |
| HOME | `-1` | 보드 외부 (아직 출발 안 한 말). |
| GOAL | `99` | 완주 (보드 외부, 가상 칸). |

⚠️ **인덱스 20, 28은 사용하지 않는다.** 표준 29칸을 단순화로 (20=0과 동일, 28은 미사용) 처리. 24/25는 centerExitB 경로 중간 칸으로 **2026-06-11 활성화됨**(§13-2 해소). (§13-8 참조)

### §2-3 ASCII 다이어그램

진행 방향: 시계 반대 방향(나무위키/위키백과 표준 — "왼쪽으로 도는" 좌선 진행) — 본 구현은 단순화를 위해 0 → 1 → 2 → ... → 19 → 0(완주) 순서로 인덱스를 증가시킨다. 인덱스 증가 방향과 회전 방향(좌선/시계반대)은 별개. board.js의 좌표는 좌하(0) → 좌상(5) → 우상(10) → 우하(15) → 좌하(=0) 순서이다.

```
       (5=좌상 모서리)       (10=우상 모서리)
            ●─── 6 ── 7 ── 8 ── 9 ───●
            │ ╲                       ╱ │
            │   21                 26   │
            │     ╲              ╱      │
            4      22         27        11
            │        ╲       ╱          │
            3          ●(23=중앙·방)    12
            │        ╱       ╲          │
            2      22*       27*       13
            │     ╱             ╲       │
            │   21*               26*   │
            │ ╱                       ╲ │
            ●── 19 ──18 ──17 ──16 ──●
       (0=좌하 모서리 = 출발/도착)    (15=우하 모서리)

  ※ 21*/22*/26*/27*는 같은 인덱스가 시각적으로 대각선 양쪽을 표현하기 위해
     반복 표시된 것이 아니라, 실제로는 21,22,26,27이 각각 한 자리씩 차지하며
     중앙(23)을 향해 한 방향으로만 진행한다. 위 다이어그램은 대각의 시각적
     교차를 강조한 단순화 그림이며, 실제 칸 수는 §2-2 표 기준.
```

진행 시퀀스 요약:
- **외곽 한 바퀴 (지름길 미사용)**: `0 → 1 → 2 → ... → 19 → 완주(GOAL)`. 총 20칸 이동 필요.
- **지름길A (좌상에서 진입)**: `... → 5 → 21 → 22 → 23(중앙) → ?`. 중앙에서 분기(§10).
- **지름길B (우상에서 진입)**: `... → 10 → 26 → 27 → 23(중앙) → ?`. 중앙에서 분기(§10).
- **centerExitB (중앙 → 좌하 출구)**: `23(중앙) → 24 → 25 → GOAL`. 잔여 steps 소진(2026-06-11, §10-4).

(구현 위치: `server.js` `advanceOneCell()`, 클라 `public/js/board.js` `OUTER_COORDS` / `SHORTCUT_A` / `SHORTCUT_B`)

---

## §3 윷가락 결과 표

### §3-1 윷가락 구성

- 윷가락 4개. 각 가락은 반원통(반달 모양 단면).
- **앞면(평평한 면)** = "배" (배가 위로 보임 = 까졌다). 영어 wiki: *flat side up*.
- **뒷면(둥근 면)** = "등" (등이 위로 보임 = 엎어졌다). 영어 wiki: *round side up*.
- 본 구현은 4개 가락 중 **1개에 빨간 X 마크**를 그려 백도(뒷도) 가락으로 사용한다 (구현: `MARKED_STICK_INDEX = 0`, `public/js/yut.js` `drawStick(..., isMarked=true)`).

### §3-2 결과 표 (표준 + 본 구현)

평평한 면(앞, 배) 개수에 따라 결과가 결정된다. 출처 — Wikipedia Yunnori:
> "The score is determined by counting the sticks that are over, and those that are up. ... One stick over (flat side up) and three sticks up (round side up) is called 'do'. ... All sticks over is called 'yut', whereas all sticks up is called 'mo'."

(주의: Wikipedia 영문판은 *over* = 앞면(flat), *up* = 뒷면(round)으로 용어를 사용한다. 한국어 표준과 시각 방향이 같다.)

| 결과 (한글) | 영문 | 앞면(평평면) 개수 | 이동 칸 수 | 보너스 던지기 | 이론 확률 (각 면 50%) | 실측 분포 (5,000회) |
|---|---|---|---|---|---|---|
| 백도(뒷도/빽도) | backdo | 1 (그 1개가 **마크된 가락**) | **−1** (뒤로) | 없음 | 1/16 ≈ **6.25%** | 6.16% |
| 도 | do | 1 (그 1개가 비마크 가락) | 1 | 없음 | 3/16 ≈ **18.75%** | 18.68% |
| 개 | gae | 2 | 2 | 없음 | 6/16 ≈ **37.5%** | 37.40% |
| 걸 | geol | 3 | 3 | 없음 | 4/16 ≈ **25%** | 24.62% |
| 윷 | yut | 4 | 4 | **있음** | 1/16 ≈ **6.25%** | 6.86% |
| 모 | mo | 0 | 5 | **있음** | 1/16 ≈ **6.25%** | 6.28% |

**핵심 직관**: 평평한 면 개수 = 이동 칸 수 (도=1, 개=2, 걸=3, 윷=4). 모만 예외(0개 → 5칸).
이 매핑이 거꾸로 구현되면 사용자가 즉시 "개가 나왔는데 1칸만 간다"라고 보고한다 (실제 Phase 2 → 2.1 핫픽스 사유, §13-4 참조).

(구현 위치: `server.js` `throwYutSticks()`)

### §3-3 백도(뒷도) 발동 조건

- 4개 가락 중 **마크된 가락(인덱스 0) 1개만** 평평면이 위로 향하고 나머지 3개가 둥근면일 때 발동.
- "도(평평면 1개)"가 발생했을 때, 그 1개가 마크된 가락이면 백도, 마크되지 않은 가락이면 일반 도.
- 전체 확률은 1/16로 고정 (백도 4개 가락 중 1개만 마크되므로 `do` 확률 4/16 중 마크 가락이 평평면인 케이스 1/4 = 1/16).

(구현 위치: `server.js` `throwYutSticks()` `switch case 1`)

### §3-4 표준에 모호한 영역

- **윷가락 종류**: 한국어 위키 — 장작윷(길이 15~20 cm, 지름 3~5 cm), 밤윷(작은 4짝)이 일반적. 5짝 윷도 일부 지역에서 사용된다. 본 구현은 **4짝 고정** (Wikipedia/위키백과의 가장 일반적인 형태).
- **확률**: 한국어 위키 — "실제 물리 윷가락은 평평면이 더 가볍고 넓어 뒷면 확률이 더 높아 불균일 확률 분포를 갖는다." 본 구현은 각 가락 50:50 균등 확률 (§13-3 참조).

---

## §4 말(馬) — 개수, 출발, 완주

### §4-1 개수

- 각 편 **4개의 말**(전통 명칭 "넉 동"). 본 구현 동일.
- 구현: `server.js` `createPiece()`로 초기화 시 4개 생성 (`p.pieces = [createPiece() × 4]`).

### §4-2 시작 상태 (HOME)

- 모든 말은 게임 시작 시 **HOME 상태**(보드 외부, 인덱스 `-1`).
- HOME 말은 첫 이동 시 보드 칸으로 진입한다.
- 본 구현 UI: P1은 좌하 코너, P2는 우상 코너의 HOME 박스에 표시 (`public/js/piece.js` `isClickOnHomeArea()`).

### §4-3 첫 이동 (HOME → 보드 진입)

- 본 구현: HOME 말에 결과 N을 적용하면 → 칸 `(N − 1)`에 도착. (HOME에서 도 = 칸 0, HOME에서 개 = 칸 1, …)
- 표준에 모호: 일부 지역 룰은 "도(1)를 쓰면 칸 0에 놓는다(=참먹이)" — 본 구현과 동일. 다른 변형은 "도가 나오면 한 칸 더 진행" 등이 있다.

(구현 위치: `server.js` `computeNextCell()` — HOME(-1) → cell 0부터 시작)

### §4-4 완주 ("났다" / "펐다")

- 한국어 위키:
  > "도에서 시작(첫 발)해 한 바퀴를 돌아 참먹이로 돌아오는 것(이것을 '났다' 또는 '펐다'라고 부른다)이 놀이의 목적이다. 단, 중요한 것은 말이 참먹이를 통과해야 나는 것이며 참먹이에서 멈추면 난 것이 아니다."
- 본 구현: 외곽 19 → 다음 외곽 진행 시 GOAL(완주). 모/윷으로 칸 수가 남아도 GOAL 처리(통과 완주).
- **주의**: 표준 룰에서는 "참먹이에서 멈추면 난 것이 아니다"라고 명시되어 있다. 본 구현에서 칸 0에 멈춘 경우와 칸 0을 통과한 경우의 처리는 §13-8 참조.

(구현 위치: `server.js` `advanceOneCell()` outer pathContext — `if (cell === 19) return GOAL`)

---

## §5 이동 규칙 + 칸 수 표

### §5-1 한 결과 = 한 묶음 이동

- 윷가락을 던져 얻은 한 결과(도/개/걸/윷/모/백도)는 **한 말(또는 한 업힘 묶음)을 한 번** 이동시키는 데 사용한다.
- 결과 큐가 여러 개 쌓인 경우(보너스 던지기 누적, §6), 사용 순서는 자유 — 한국어 위키:
  > "한 번 더 또는 그 이상을 던져 나온 윷들은 모두 순서를 바꾸어 사용할 수 있다."
- 본 구현: 사용자가 결과 칩을 클릭으로 선택 후 말을 클릭하여 이동. (`public/js/ui.js` 결과 큐 칩 클릭 핸들러)

### §5-2 칸 수 표 (외곽 진행 기준)

| 결과 | 이동 칸 수 | 예: 칸 3에서 사용 시 도착 칸 |
|---|---|---|
| 백도 | −1 | 칸 2 |
| 도 | 1 | 칸 4 |
| 개 | 2 | 칸 5 (좌상 모서리 — 다음 이동 시 지름길A 자동 진입, §10) |
| 걸 | 3 | 칸 6 |
| 윷 | 4 | 칸 7 + 보너스 던지기 |
| 모 | 5 | 칸 8 + 보너스 던지기 |

### §5-3 묶음(업힘) 일괄 이동

- 같은 칸에 있는 자기 편 말은 **한 묶음**으로 묶여 다음 이동 시 함께 움직인다.
- 본 구현은 별도 "묶음 객체"를 만들지 않고, 이동 시 `movePiece()` 함수가 시작 칸이 같은 자기 말 인덱스를 모아 `groupIndices`로 일괄 이동시킨다 (`server.js` `movePiece()` 묶음 처리 로직).

### §5-4 표준에 모호한 영역

- **결과 사용 순서**: 위키 — 자유 순서. 본 구현 동일.
- **출발선 처리**: 한 결과로 HOME에서 보드 진입 후, 이어지는 결과로 묶음(업힘)을 한꺼번에 이동시킬 수 있는지는 룰마다 다르다. 본 구현은 묶음 처리가 시작 칸 기준으로 묶이므로 같은 턴 내에 묶음 가능.

---

## §6 보너스(한 번 더) 던지기

### §6-1 보너스 발생 조건 (표준)

한국어 위키 — 윷을 한 번 더 던지는 기회:
1. **윷**이 나온 경우
2. **모**가 나온 경우
3. **상대 편 말을 잡은 경우** (단, 잡기 결과가 윷·모면 중복 보너스 없음 — 한 번만 다시 던짐)

> 한국어 위키 인용:
> > "윷이나 모를 이용하여 다른 참가자의 행마를 잡은 경우에는 중복해서 다시 던지기를 할 수 없다.(예, 윷이나 모로 잡은 경우, 두 번이 아닌 한 번만 다시 던질 수 있다)"

### §6-2 본 구현

- 윷·모 → `bonus: true` 플래그로 즉시 추가 던지기 허용 (`server.js` `throwYutSticks()` — `bonus: result === 'yut' || result === 'mo'`).
- 잡기 보너스 → `game.capturedBonus = true` 플래그 (`server.js` MOVE_PIECE 핸들러 `if (moveRes.captured) game.capturedBonus = true`).
- 큐가 비어도 `lastResult === 'yut' || 'mo'`이거나 `capturedBonus === true`이면 다시 던질 수 있다 (THROW_YUT 진입 검사).
- 백도는 보너스 없음.

### §6-3 보너스 누적

- 윷·모를 연속 N회 내면 결과가 큐에 N개 누적된다.
- 모든 결과를 사용해야 차례가 넘어간다 (`server.js` MOVE_PIECE 마지막 부분 — `if (pendingResults.length === 0 && !hasBonus) passTurn()`).

### §6-4 표준 vs 구현 차이

⚠️ 한국어 위키 명시: "**윷/모로 잡은 경우 중복 보너스 없음** — 한 번만 다시 던짐."
본 구현은 **2026-06-15부로 §13-12 해소** — `MOVE_PIECE`/`CHOOSE_PATH`에서 사용 결과(`useResult`)가 yut/mo이면 잡기가 발생해도 `capturedBonus`를 부여하지 않는다(`if (moveRes.captured && useResult !== 'yut' && useResult !== 'mo') game.capturedBonus = true;`). 윷·모를 던진 것 자체가 이미 추가 던지기 권리 1회를 부여하므로 잡기 보너스는 더해지지 않는다(중복 차단). 도/개/걸로 잡기는 자체 보너스가 없으므로 `capturedBonus` +1을 유지한다.

**capturedBonus 소진 타이밍 정밀화 (FIX-4, 2026-06-11)**: `THROW_YUT`에서 `capturedBonus` 소진을 **"큐가 빈 상태에서 capturedBonus 권리로 진입한 던지기"** 1회에 한정한다(`enteredViaCapturedBonus = pendingResults.length === 0 && capturedBonus === true`). 윷·모 보너스 권리로 진입한 던지기(큐에 yut/mo 잔여)에서는 capturedBonus를 **보존**한다. 이전에는 capturedBonus가 true이면 무조건 소진되어 잡기 보너스 권리를 부당하게 잃던 엣지 케이스를 해소.

---

## §7 잡기

### §7-1 표준 룰

- 이동 후 상대 편 말이 있는 칸에 도착하면 **잡기** 발생.
- 잡힌 상대 말(업힘 묶음 포함 **전부**)은 HOME(출발 전 상태)으로 되돌아간다.
- 잡은 편은 보너스 던지기 1회 획득(§6).

> 영문 Wikipedia 인용:
> > "If a mal lands on a station occupied by the opponent's team, the opponent's mal is removed from the course and returned to the starting position, and the current player is allowed to cast again."
> > "If an opponent lands their mal on a station occupied by a group of mals of the opponent, all mals in the group are removed from the course."

### §7-2 본 구현

- `server.js` `resolveLanding()`:
  - 같은 cell에 있는 상대 piece를 순회하며 `op.cell = HOME; op.stack = 1`로 전부 HOME 복귀.
  - `oppCaught > 0`이면 `captured = true` 반환 → MOVE_PIECE 핸들러에서 `game.capturedBonus = true`.
- 업힘 묶음 잡기: 같은 cell의 모든 상대 piece가 일괄 HOME 복귀 (코드: `for (let i = 0; i < opp.pieces.length; i++)`).

### §7-3 표준에 모호한 영역

- 잡힌 말이 HOME으로 복귀할 때 "출발선(칸 0)에 놓이는지 / HOME으로 가는지" 변형이 있다. 본 구현은 **HOME(−1)** 복귀 (보드 외부).
- 한국어 위키:
  > "참먹이에서 멈추면 난 것이 아니다. 상대편 말이 참먹이에서 멈췄다면 참먹이에서 잡을 수 있다."
- 본 구현은 칸 0(참먹이)에서 잡기가 발생하면 동일 처리 (잡힌 말 HOME 복귀 + 보너스).

---

## §8 업기

### §8-1 표준 룰

- 이동 후 자기 편 말이 있는 칸에 도착하면 **업기** 발생 → 같은 칸의 자기 말이 묶음으로 합쳐져 이후 함께 이동한다.
- 업힌 묶음이 상대에게 잡히면 묶음 전체가 HOME으로 복귀(§7).

### §8-2 본 구현

- 본 구현은 **명시적인 "업힘 객체"를 만들지 않는다.** 대신 `movePiece()` 호출 시 시작 칸이 동일한 자기 piece를 묶음으로 일괄 이동시킨다.
- 시각적으로는 같은 칸의 piece 카운트(stack) 합계를 표시 (`public/js/ui.js` 같은 cell의 piece 개수 표기).
- 코드:
  ```js
  // server.js movePiece()
  if (startCell !== HOME && startCell !== GOAL) {
    for (let i = 0; i < mover.pieces.length; i++) {
      if (i === pieceIdx) continue;
      const mp = mover.pieces[i];
      if (!mp.done && mp.cell === startCell) {
        groupIndices.push(i);
      }
    }
  }
  ```

### §8-3 표준에 모호한 영역

- **업힘 묶음 분리**: 위키 — 업힌 말을 다시 분리하여 따로 이동시키는 변형 룰은 명시되지 않음. 본 구현은 같은 칸의 자기 piece가 무조건 함께 이동 — **분리 불가**.
- **자기 말 칸 통과 시 업힘 발생 여부**: 본 구현은 **도착 칸에 있을 때만** 업힘 발생. 통과 칸은 무영향.

---

## §9 백도(뒷도/빽도)

### §9-1 표준 룰 (변형 룰)

- 한국어 위키:
  > "윷 중 특정 윷에 표시가 되어있는 윷의 앞부분이 보여 도가 나왔을 경우 말을 한 칸 뒤로 물린다."
- 표준 룰의 **변형(옵션)**. 모든 지역에서 사용하지 않으며 인정 여부는 지역에 따라 다르다.
- 발동 조건: 4개 가락 중 마크된 1개만 평평면이 위로 향함 (= "도"인데 그 1개가 마크 가락).
- 이동: 1칸 뒤로.

### §9-2 본 구현 — 적용 규칙 모음

본 구현은 백도를 **기본 활성화**하며 아래 규칙을 적용한다 (인덱스 0번 가락이 마크 가락).

| 상황 | 처리 |
|---|---|
| 출발한 말(보드 위)에 백도 | 외곽 범용: `cell - 1`로 1칸 뒤로 (범위 cell 2~18). `server.js` `computeNextCell()` `steps === -1` 분기 |
| **첫칸(cell 1)에서 백도** | 외곽 마지막 칸 `19`로 **워프** (한 바퀴 돌아온 것처럼 처리, done=false). 2026-06-15 §13-5 해소 |
| **외곽 마지막 칸(cell 19)에서 백도** | 첫칸 `1`로 워프 복귀 (대칭 규칙). 2026-06-15 §13-5 해소. 이전 단순 후퇴(19→18) 대체 |
| **HOME 말에 백도** | 사용 불가 → 서버가 자동 폐기 (사용자에게 결과는 알려주되 큐에 안 넣음). `THROW_YUT` 핸들러 `canUseBackdo` 체크 |
| 칸 0(출발선)에서 백도 | 그대로 칸 0 유지 (뒤로 못 감) |
| 지름길 안에서 백도 | 진입 모서리로 복귀: `21 → 5`, `22 → 21`, `26 → 10`, `27 → 26` |
| centerExitB 안에서 백도 | 진입 방향으로 복귀: `24 → 23`, `25 → 24` (2026-06-11 신설) |
| 중앙(23)에서 백도 | `22`로 복귀 (지름길A 경로 가정 — **표준에 명확한 정의 없음**, §13-6 참조) |
| 보너스 턴 | 없음 |

### §9-3 백도 후 출발선 통과 처리 (표준에 모호)

- 한국어 위키:
  > "도를 한 뒤 뒷도가 나와 출발로 오게 된 말이 바로 날 수 있는지도 규칙마다 다르다."
- 본 구현은 칸 0에서 백도 발동 시 칸 0 유지(완주 처리 안 함). 도→백도로 칸 0에 돌아온 말은 한 바퀴 더 돌아야 완주.

(구현 위치: `server.js` `computeNextCell()` `steps === -1` 분기)

---

## §10 분기점(모서리 + 중앙) 처리

### §10-1 표준 룰 — 모서리 분기

- 한국어 위키:
  > "윷판에서 말이 갈 수 있는 길은 모두 네 가지가 있다. … 모서리에서는 사선으로 이동 선택 가능."
- 좌상(`5`) 또는 우상(`10`) 모서리에 **정확히 멈춘 다음** 이동할 때, **외곽 계속 vs 지름길 진입 중 선택**할 수 있는 것이 표준.
- 모서리를 단순 통과(멈추지 않음)하면 외곽으로 계속 진행 — 지름길 미사용.

### §10-2 본 구현 — 외곽/지름길 선택 분기 (§13-1 해소, 2026-06-11)

- 본 구현은 모서리(5, 10)에 정확히 멈춘 다음 이동 시 **분기 모달을 표시**하여 "외곽 계속 / 지름길 진입"을 선택하게 한다(정통 룰 정합, FIX-2).
- 코드 근거:
  ```js
  // server.js computeNextCell()
  if (cell === 5) {
    if (!branchChoice) return { toCell: 5, awaitingBranch: true };  // 분기 대기
    pathContext = branchChoice === 'shortcut' ? 'shortcutA' : 'outer';
  } else if (cell === 10) {
    if (!branchChoice) return { toCell: 10, awaitingBranch: true };
    pathContext = branchChoice === 'shortcut' ? 'shortcutB' : 'outer';
  }
  ```
- 모서리에 멈춘 말이 다음 이동을 시도하면 `awaitingBranch: true`를 반환 → 서버가 `BRANCH_REQUEST(branchType: 'corner')` 발송 → 클라가 "↩ 외곽 계속 / ↗ 지름길 진입" 모달 표시 → `CHOOSE_PATH(pathChoice: 'outer'|'shortcut')`로 응답.
- **모서리 통과**(멈추지 않음) 이동은 분기 없이 외곽 유지(기존 동작 불변).
- **중첩 분기**: 모서리에서 "지름길 진입"을 선택했는데 잔여 steps가 중앙(23)을 통과하면, 중앙 분기 모달이 **연속으로** 표시된다(2단계 모달). 서버는 모서리 대기 중 수신한 `top`/`bottom`을 `shortcut-top`/`shortcut-bottom` 복합값으로 합성하여 큐 중복 차감 없이 출구까지 진행한다(§10-3).

### §10-3 중앙(방) 분기

- 중앙(`23`)에 도달하면 **두 출구 중 선택**:
  - **centerExitA (top)**: 중앙 → 외곽 15(우하) → 16 → 17 → 18 → 19 → GOAL
  - **centerExitB (bottom)**: 중앙 → 24 → 25 → GOAL (잔여 steps 소진, 2026-06-11 §10-4)
- 본 구현은 항상 클라이언트 모달로 선택 (`server.js` `awaitingBranch: true` 응답 → 클라이언트 BRANCH_REQUEST(branchType: 'center') 처리 → `CHOOSE_PATH(pathChoice: 'top'|'bottom')` 응답).

### §10-4 중앙→좌하 출구 24/25 경유 (§13-2 해소, 2026-06-11)

- 본 구현 centerExitB는 중앙(23)에서 **24 → 25 → GOAL** 순으로 진행하며 잔여 steps를 소진한다(전통 "날밭" 2칸):
  ```js
  // server.js advanceOneCell() centerExitB
  if (cell === 23) return 24;
  if (cell === 24) return 25;
  if (cell === 25) return GOAL;
  return GOAL;
  ```
- 예: 중앙에서 도(1) → 24, 개(2) → 25, 걸(3) → GOAL. 모(5) 등 잔여가 GOAL을 넘으면 GOAL 흡수.
- 24/25에서 잡기/업기/백도 정상 동작(`resolveLanding`이 cell 기준 처리). 백도 복귀: `25 → 24`, `24 → 23`(§9-2).
- board.js `buildCenterExitB()`가 중앙(23)→좌하(0) 대각선 1/3·2/3 지점에 24/25 셀을 렌더링한다(AD3 APPROVED).

### §10-5 중앙에서 출구 선택 시점 (진입 경로별 출구)

- 표준 룰: 지름길A로 들어온 말은 좌상→중앙→우하 경로가 일반적, 지름길B로 들어온 말은 우상→중앙→좌하 경로가 일반적. 즉 **진입 경로에 따라 출구가 어느 정도 고정**되는 지역이 다수다.
- 본 구현(2026-06-15 §13-6 해소):
  - **지름길B(우상10→26→27→) 경유로 중앙(23)에 정확히 정착**한 말은 다음 이동 시 분기 모달(BRANCH_REQUEST) 없이 **자동으로 centerExitB(bottom, 23→24→25→GOAL)** 출구를 사용한다. 정통 룰의 "우상→중앙→좌하" 경로 고정을 반영.
  - **지름길A(좌상5→21→22→) 경유** 정착 말은 기존대로 **top/bottom 자유 선택**(BRANCH_REQUEST center 모달)을 유지한다.
  - 중앙 **통과**(중첩 분기, 잔여 steps 있음)는 기존 `shortcut-top`/`shortcut-bottom` 합성으로 처리(§13-1 중첩 분기). 자동 라우팅은 `piece.cell===23` 정확 정착 케이스에만 한정되므로 충돌하지 않는다.
  - 진입 경로 추적: `computeNextCell` 반환 `finalPath`(착지 pathContext) → `piece.lastPath`에 저장(서버 내부 필드, STATE 미노출). `lastPath==='shortcutB' && cell===23`이면 자동 bottom.

---

## §11 승리 조건

### §11-1 표준 룰

- 자기 편 말 4개 모두 완주("났다") 시 승리.
- 영문 Wikipedia:
  > "The game is won by the team who brings all their mals home first, that is complete the course with all their mals."

### §11-2 본 구현

- `server.js` `resolveLanding()` — `const allDone = mover.pieces.every((p) => p.done);` `finished = allDone`.
- MOVE_PIECE / CHOOSE_PATH 핸들러에서 `if (moveRes.finished)` → `game.winner = player.id; game.started = false; broadcastAll({ type: 'GAME_OVER', winner })`.
- 완주 후 남은 결과 큐가 있어도 즉시 게임 종료.

### §11-3 모/윷 직접 완주

- 본 구현: 완주 칸을 넘어서도 (예: 칸 17에서 모(5) 사용 시) 완주 처리. `advanceOneCell()`의 `if (cell === 19) return GOAL` 분기.
- 표준 룰: "참먹이를 통과해야 난다"는 정의에 따라 동일 — 본 구현 OK.

---

## §12 QA 체크리스트

> 본 체크리스트는 matgo `tests/` Playwright 패턴을 따른다. 룰북 기반 시나리오 작성 시 §번호를 인용한다.

### §12-1 이동 칸 수 검증 (§3, §5)

- [ ] **§5-2 도(1)**: 칸 3에서 도 사용 → 칸 4 도착 확인
- [ ] **§5-2 개(2)**: 칸 3에서 개 사용 → 칸 5 도착 확인
- [ ] **§5-2 걸(3)**: 칸 3에서 걸 사용 → 칸 6 도착 확인
- [ ] **§5-2 윷(4)**: 칸 3에서 윷 사용 → 칸 7 도착 + 보너스 던지기 발동 확인 (§6)
- [ ] **§5-2 모(5)**: 칸 3에서 모 사용 → 칸 8 도착 + 보너스 던지기 발동 확인 (§6)
- [ ] **§9-2 백도(-1)**: 칸 3에서 백도 사용 → 칸 2 도착 확인

### §12-2 윷가락 매핑 회귀 검증 (§3, §13-4)

- [ ] **§3-2 매핑**: 4개 가락 전부 평평면(앞) → 윷(4칸) 확인 (Phase 2.1 회귀 방지)
- [ ] **§3-2 매핑**: 4개 가락 전부 둥근면(뒤) → 모(5칸) 확인
- [ ] **§3-2 분포**: 5,000회 던지기 분포 — 이론값 ±3% 범위 내 확인

### §12-3 백도 (§9)

- [ ] **§9-2 HOME 백도**: HOME 말만 있는 상태에서 백도 결과 → 자동 폐기 + 큐 비어 있고 보너스 없으면 턴 종료 확인
- [ ] **§9-2 칸 0 백도**: 칸 0에서 백도 → 칸 0 유지 확인 (뒤로 못 감)
- [ ] **§9-2 지름길 백도**: 칸 21 → 5, 22 → 21, 26 → 10, 27 → 26 복귀 확인
- [ ] **§9-2 중앙 백도**: 중앙(23)에서 백도 → 칸 22 복귀 확인 (구현 정책)
- [ ] **§9-2 백도 마크**: 윷가락 4개 중 인덱스 0 가락에 빨간 X 표식 시각 확인

### §12-4 잡기/업기 (§7, §8)

- [ ] **§7-1 잡기**: 상대 단독 말이 있는 칸에 도착 → 상대 말 HOME 복귀 + 보너스 던지기 확인
- [ ] **§7-1 묶음 잡기**: 상대 업힘 묶음(2개 이상) 도착 → 묶음 전체 HOME 복귀 확인
- [ ] **§8-2 업기**: 같은 칸 자기 말 도착 → 다음 이동 시 함께 이동 확인
- [ ] **§8-2 업힌 묶음 잡힘**: 자기 묶음이 상대에게 잡힘 → 전체 HOME 복귀 확인

### §12-5 분기 (§10)

- [ ] **§10-2 모서리(5)**: 정확히 칸 5에 멈춤 → 다음 이동 시 BRANCH_REQUEST(corner) + 모달 표시 확인
- [ ] **§10-2 모서리(10)**: 정확히 칸 10에 멈춤 → 다음 이동 시 BRANCH_REQUEST(corner) 확인
- [ ] **§10-2 모서리 외곽 선택**: CHOOSE_PATH(outer) → 다음 외곽 칸 진행 확인 (5→6, 10→11)
- [ ] **§10-2 모서리 지름길 선택**: CHOOSE_PATH(shortcut) → 지름길 진입 확인 (5→21, 10→27)
- [ ] **§10-2 모서리 통과**: 모서리를 단순 통과 → 외곽 계속 진행, 분기 없음 확인
- [ ] **§10-2 중첩 분기**: 모서리에서 지름길 선택 + 중앙 통과 → corner 모달 → center 모달 연속 표시, 큐 1회만 차감 확인
- [ ] **§10-3 중앙 도달**: 칸 23 도착 시 BRANCH_REQUEST(center) + 모달 표시 확인
- [ ] **§10-3 centerExitA**: 중앙 → 15 → 16 → 17 → 18 → 19 → GOAL 경로 확인
- [ ] **§10-4 centerExitB**: 중앙에서 bottom 선택 → 24 → 25 → GOAL 잔여 소진 확인
- [ ] **§10-4 centerExitB 잡기/업기**: 24/25 칸에서 잡기/업기 정상 동작 확인
- [ ] **§9-2 centerExitB 백도**: 24 → 23, 25 → 24 복귀 확인

### §12-6 보너스 (§6)

- [ ] **§6-2 윷 보너스**: 윷 후 큐 비어도 던지기 가능 확인
- [ ] **§6-2 모 보너스**: 모 후 큐 비어도 던지기 가능 확인
- [ ] **§6-2 잡기 보너스**: 잡기 후 `capturedBonus` true → 큐 비어도 던지기 가능 확인
- [ ] **§6-2 보너스 누적**: 윷·모 연속 → 결과 큐에 여러 개 누적 확인
- [ ] **§6-4 capturedBonus 소진**: 큐 빈 상태 + capturedBonus=true로 THROW → 소진(false) 후 정상 턴 흐름 확인 (FIX-4)
- [ ] **§6-4 capturedBonus 보존**: 큐에 yut/mo 잔여 + capturedBonus=true로 THROW → capturedBonus 보존 확인 (FIX-4)
- [ ] **§6-4 잡기+윷/모**: 윷·모로 잡은 경우 보너스 중복 차단 — capturedBonus 미부여(§13-12 해소 2026-06-15, YR-C19-001/002/006). 도/개/걸로 잡기는 capturedBonus 부여 유지(YR-C19-003/004/005)

### §12-7 승리 (§11)

- [ ] **§11-2 4말 완주**: 마지막 말 완주 시 즉시 GAME_OVER + winner broadcast 확인
- [ ] **§11-2 잔여 큐**: 완주 후 남은 결과 큐가 있어도 즉시 종료 확인
- [ ] **§11-3 통과 완주**: 칸 17에서 모 사용 → 칸 19 통과 → GOAL 처리 확인

### §12-8 프로토콜 (§부록)

- [ ] **STATE 동기**: 매 액션 후 양쪽 클라이언트에 STATE broadcast 확인
- [ ] **턴 검증**: 상대 턴에 THROW_YUT 시도 → ERROR 응답 확인
- [ ] **큐 검증**: 사용할 수 없는 결과명 MOVE_PIECE → ERROR 응답 확인
- [ ] **분기 대기**: `awaitingBranchAt !== null` 상태에서 THROW_YUT 시도 → ERROR 확인

---

## §13 구현 노트 (구현 vs 표준 차이)

> 본 섹션은 Planner가 코드 정독 결과 발견한 **구현과 표준의 차이**와, 사용자가 "뭔가 이상하다"고 보고한 의심 후보를 강조한다.
> 영향도: **HIGH** = 즉시 직관 불일치 / **MED** = 특정 시점에 혼란 발생 / **LOW** = 무영향에 가깝거나 의도된 단순화
> 현재 12건 (미해소 4 + 해소 8). 2026-06-11 §13-1/§13-2 해소, §13-12 신규 등록. 2026-06-15 §13-12·§13-5·§13-6 해소.

### §13-1 모서리 분기: 강제 지름길 vs 선택 [HIGH, **2026-06-11 해소**] — 사용자 의심 후보 1순위(해소됨)

| 영역 | 모서리 분기 (§10-2) |
|---|---|
| **표준 룰 (출처: 한국어 위키)** | "모서리에서는 사선으로 이동 선택 가능" — 모서리에 정확히 멈추면 다음 이동 시 외곽 계속 vs 지름길 진입 **선택**. |
| **이전 구현** | `server.js` `computeNextCell()` — `cell === 5`이면 `pathContext = 'shortcutA'`, `cell === 10`이면 `'shortcutB'`로 **고정**. 선택지 모달 미표시. |
| **현재 구현 (2026-06-11, FIX-2)** | 모서리에서 `branchChoice=null`이면 `awaitingBranch: true` 반환 → `BRANCH_REQUEST(branchType: 'corner')` 발송 → 클라 "외곽 계속 / 지름길 진입" 모달 → `CHOOSE_PATH('outer'\|'shortcut')`. 모서리에서 지름길 선택 + 중앙 통과 시 corner→center 2단계 모달(중첩 분기), 서버가 `shortcut-top`/`shortcut-bottom` 복합값 합성으로 큐 1회만 차감. |
| **영향도** | **HIGH (해소됨)** |
| **사용자 영향** | 이전: 모서리에 멈추면 외곽으로 계속 가고 싶어도 강제 지름길로 빨려 들어감. 현재: 외곽/지름길 자유 선택. |
| **상태** | **2026-06-11 해소**. 신규 회귀 `rulebook-c16-corner-branch.spec.js`(YR-C16-001~011) + `qa-rulefix-edge.spec.js`(QA-RF2/RFX). 추적성을 위해 항목 유지. |

---

### §13-2 중앙→좌하 출구(centerExitB): 즉시 완주 [HIGH, **2026-06-11 해소**] — 사용자 의심 후보 2순위(해소됨)

| 영역 | 중앙 분기 좌하 출구 (§10-4) |
|---|---|
| **표준 룰** | 중앙(방)에서 좌하 방향으로 진행할 경우 외곽 합류 후 출발선(참먹이)을 통과해야 완주. 중앙과 좌하 사이에 1~2개의 칸(전통 명칭 "쨀밭", "날밭" 등)을 더 거치는 것이 일반적. |
| **이전 구현** | `server.js` `advanceOneCell()` `centerExitB` — `if (cell === 23) return GOAL;` 즉시 완주. 남은 steps 무관. |
| **현재 구현 (2026-06-11, FIX-3)** | `centerExitB` 경로 `23 → 24 → 25 → GOAL` 잔여 steps 소진. 24/25 신규 활성화(전통 "날밭"). 24/25에서 잡기/업기/백도 정상 동작, 백도 복귀 `25 → 24`, `24 → 23`. board.js `buildCenterExitB()` 좌표·렌더링·경로선 추가. |
| **영향도** | **HIGH (해소됨)** |
| **사용자 영향** | 이전: 중앙에서 bottom 선택 후 모(5칸)를 써도 즉시 완주("말이 갑자기 사라졌다"). 현재: 24/25를 경유하며 잔여 칸을 소진하므로 잡힐 기회·전략 깊이 회복. |
| **상태** | **2026-06-11 해소**. 신규 회귀 `rulebook-c17-center-exit-b.spec.js`(YR-C17-001~009) + `qa-rulefix-edge.spec.js`(QA-RF3). AD3 APPROVED(24/25 렌더링). 추적성을 위해 항목 유지. |

---

### §13-3 윷가락 확률: 균등 50% [LOW]

| 영역 | 확률 분포 (§3-2) |
|---|---|
| **표준 룰 (출처: 한국어 위키)** | "윷가락의 단면이 반달 모양 즉 반원이어서, 평평한 면(배)이 위로 되는 확률과 둥근면(등)이 위로 되는 확률이 윷을 던지는 방법에 따라서 다르다." 실제 물리 가락은 약 60~65%로 뒷면(둥근면) 편향이 일반적. |
| **현재 구현** | `Math.random() < 0.5` — 정확히 50:50. (`server.js` `throwYutSticks()`) |
| **영향도** | **LOW** |
| **사용자 영향** | 개(2) 빈도가 표준 물리 가락 대비 낮고, 윷/모 빈도가 약간 높음 (실측 분포 §3-2 표 참조). LAN 친구 게임 수준에서는 허용 범위. |
| **해결 방향** | **정책 명기**: 디지털 윷놀이는 의도적으로 균등 분포가 일반적(테이블에 가락 없음). 수정 불요. |

---

### §13-4 윷가락 매핑 이력 (Phase 2 → 2.1 핫픽스) [현재 해소됨, MED 위험]

| 영역 | 매핑 (§3-2) |
|---|---|
| **표준 룰** | 평평면 개수 = 이동 칸 수 (도=1, 개=2, 걸=3, 윷=4). 모만 예외(0개 → 5칸). |
| **Phase 2 (오류 매핑)** | `fronts=0→윷, 1→걸, 2→개, 3→도/백도, 4→모` (정통 룰의 정반대) |
| **Phase 2.1 (정정)** | `fronts=0→모, 1→도/백도, 2→개, 3→걸, 4→윷` (정통 룰) |
| **현재 구현 (server.js)** | Phase 2.1과 동일 — **정통 룰 매핑 올바름** |
| **영향도** | **MED (회귀 위험)** — 현재 해소됨. 단, CLAUDE.md "변경 시 자주 깨지는 함정" 섹션에 매핑 함정으로 명시됨. |
| **사용자 영향** | 매핑이 뒤집히면 즉시 "개가 나왔는데 1칸만 간다"라고 보고됨 (실제 Phase 2 발견 경위). 회귀 방지 테스트가 필수. |
| **해결 방향** | **회귀 테스트 유지**: §12-2 5,000회 분포 검증. Coder가 `throwYutSticks()` 수정 시 반드시 본 §13-4를 확인. |

---

### §13-5 첫칸 빽도 워프 [LOW] — 2026-06-15 해소

| 영역 | 첫칸(cell 1) 빽도 (§9-2) |
|---|---|
| **표준 룰** | 일부 지역 룰 — 첫칸(출발 직후)에서 빽도가 나오면 한 바퀴를 거의 다 돌아온 것처럼 외곽 마지막 칸으로 워프(되돌아감). |
| **이전 구현** | cell 1 + backdo → cell 0 (단순 1칸 후퇴). 워프 미적용. |
| **현재 구현(2026-06-15 해소)** | `computeNextCell` `steps === -1` 분기에 첫칸 특례 추가 — `cell 1 → cell 19`(워프, done=false), `cell 19 → cell 1`(워프 복귀, 대칭). 외곽 범용 후퇴 범위를 `2~18`로 좁혀 언더플로우 방어. 워프 후 도(1) 이상 이동 시 기존 `advanceOneCell(19→GOAL)`로 자연 완주. |
| **영향도** | **LOW (해소)** |
| **회귀** | YR-C5-001/010/011/012/013, U-56/56b/65. cell 0·HOME·지름길·centerExitB 백도는 미접촉(아래 HOME 폐기 항목 유지). |

**참고 — HOME 백도 자동 폐기(의도적 단순화, 유지)**: HOME 말만 있을 때 백도는 `THROW_YUT` 핸들러 `canUseBackdo` 검사로 자동 폐기(결과는 클라에 알리되 큐에 안 넣음). 본 워프 해소와 무관하게 기존 정책 유지. UI에서 "백도 사용 불가 — 자동 폐기" 토스트 권장.

---

### §13-6 중앙 분기 진입 경로별 출구 [LOW] — 2026-06-15 해소

| 영역 | 중앙 출구 선택 (§10-5) |
|---|---|
| **표준 룰** | 일부 지역은 진입 경로에 따라 출구 고정 (지름길A 진입 → 우하 출구, 지름길B 진입 → 좌하 출구). 다른 지역은 자유 선택. |
| **이전 구현** | 진입 경로 무관 항상 top/bottom 양방향 자유 선택. |
| **현재 구현(2026-06-15 해소)** | **지름길B 경유로 중앙(23)에 정확히 정착**(`piece.lastPath==='shortcutB' && cell===23`)한 말은 다음 이동 시 BRANCH_REQUEST 없이 **자동 centerExitB(bottom)** 적용. 진입 경로 추적은 `computeNextCell` 반환 `finalPath` → `piece.lastPath`(서버 내부 필드, STATE 미노출). 지름길A 경유는 기존 자유 선택 유지. 중앙 통과(중첩 분기)는 §13-1 `shortcut-bottom` 합성으로 처리(자동 라우팅과 충돌 없음 — cell 5/10 vs cell 23 조건 분리). `server.js` MOVE_PIECE 핸들러(자동 분기) + CHOOSE_PATH 핸들러(방어 분기). |
| **영향도** | **LOW (해소)** |
| **회귀** | YR-C7-008(자동 bottom 갱신)/011/012, YR-C16-011·QA-RF2 그룹(중첩 분기 무영향), 봇 smoke 7/7. |

---

### §13-7 선후공 결정 절차 생략 [LOW, 의도적]

| 영역 | 선공 결정 (§1) |
|---|---|
| **표준 룰 (출처: 영문 Wikipedia)** | "The start of the game is determined by each team casting the yut-sticks. The team with the highest score starts first." (양 편이 첫 던지기를 해서 더 큰 결과를 낸 편이 선공) |
| **현재 구현** | `resetGame()` — `game.currentTurn = 'p1'`로 무조건 p1 선공. |
| **영향도** | **LOW** |
| **사용자 영향** | 친구 1:1 게임에서는 두 명이 합의하면 되므로 큰 문제 없음. 단, 공정성 의식이 있다면 신경 쓰일 수 있음. |
| **해결 방향** | **정책 명기**: LAN 친구 게임 단순화. 향후 "선공 결정" 모달을 추가하려면 별도 발주. |

---

### §13-8 외곽 칸 인덱스 매핑 단순화 [LOW]

| 영역 | 인덱스 매핑 (§2-2) |
|---|---|
| **표준 윷판** | 29칸 (외곽 20 + 대각 8 + 중앙 1). 표준 명칭이 칸마다 부여됨 (참먹이, 모, 방, 찌, 쨀, 날밭, …). |
| **현재 구현 (2026-06-11)** | 인덱스 `0, 1..19` (외곽 20), `21, 22, 26, 27` (지름길), `23` (중앙), `24, 25` (centerExitB, 2026-06-11 활성화). **인덱스 20, 28만 사용 안 함**. 명칭은 인덱스로만 식별. |
| **영향도** | **LOW** |
| **사용자 영향** | 무영향. 코드 가독성 측면에서 미사용 인덱스가 혼란을 줄 수는 있음. |
| **해결 방향** | **무영향**: 인덱스 20을 칸 0과 동일 취급(외곽 한 바퀴 = 20칸)하는 단순화는 표준 룰에 합당. 24/25는 §13-2 해소(FIX-3)로 centerExitB 중간 칸에 활용됨. |

---

### §13-9 HOME 시작 위치 좌우 분리 [LOW, **2026-05-31 해소**]

| 영역 | HOME 시각 위치 (§4-2) |
|---|---|
| **표준 룰** | 양 편 모두 **같은 출발점(참먹이, 좌하)**을 공유. 시작 위치 좌우 분리 개념 없음. |
| **이전 구현** | P1은 좌하 코너, P2는 **우상 코너**의 HOME 박스에 표시 (`public/js/ui.js`). 시각적으로 양 편이 보드 반대편에서 출발하는 듯한 인상. |
| **현재 구현 (2026-05-31)** | **양 팀 모두 좌하 출발점 근처에 같이 배치** (`public/js/ui.js`). 정통 룰과 일치. |
| **영향도** | **LOW (해소됨)** |
| **사용자 영향** | 이전: "P2가 왜 반대편에서 시작하지?"라는 직관 충돌. 게임 로직(첫 칸 진입 인덱스)에는 영향 없었으나 시각적 혼란 유발. 현재: 해소. |
| **상태** | **2026-05-31 해소**. `public/js/ui.js` HOME 박스 좌표 통일. 추적성을 위해 항목 유지. |

---

### §13-10 HOME 첫 카운트 1칸 차이 (HOME → 칸 N-1 단순화) [MED, **2026-05-31 해소**]

| 영역 | HOME → 보드 진입 칸 수 (§4-3, §5-2) |
|---|---|
| **표준 룰** | HOME 말에 결과 N을 적용하면 N칸을 모두 소진하여 칸 N에 도착해야 정통. (HOME → 도 = 칸 1, HOME → 걸 = 칸 3, HOME → 모 = 칸 5) |
| **이전 구현** | `server.js` `advanceOneCell()`: `if (cell === -1) return 0` — HOME에서 1칸 진행 시 칸 0(출발선)에 머무름. 결과적으로 HOME에서 N칸 = 칸 (N-1) 도착. 정통과 **1칸 차이**. |
| **현재 구현 (2026-05-31)** | `advanceOneCell()`: `if (cell === -1) return 1`로 변경 + `if (cell === 0) return 1` 안전망 추가. HOME → 도 = 칸 1, 걸 = 칸 3, 윷 = 칸 4, 모 = 칸 5. 정통 룰 정합. |
| **영향도** | **MED (해소됨)** — 진입 칸 차이가 누적되어 잡기/업기/완주 타이밍에 영향. |
| **사용자 영향** | 이전: HOME 출발 말이 항상 1칸 적게 도착하여 "걸(3) 썼는데 2칸밖에 안 갔다"는 보고 발생. 현재: 해소. |
| **상태** | **2026-05-31 해소**. §5-2 칸 수 표는 보드 위 진행 기준이므로 본 항목과 무관하게 유지. 추적성을 위해 항목 유지. |

---

### §13-11 capturedBonus 잔류 위험 (MOVE_PIECE passTurn 후 미리셋) [HIGH, **2026-05-31 해소**]

| 영역 | 잡기 보너스 플래그 라이프사이클 (§6-2) |
|---|---|
| **표준 룰** | 잡기 보너스는 해당 턴 내 1회 추가 던지기로 소진된다. 다음 턴으로 넘어가면 보너스는 무효. |
| **이전 구현** | `server.js` MOVE_PIECE 핸들러 (line 828 부근) — `passTurn()` 호출 후 `game.capturedBonus = false` 리셋이 **누락**. 잡기 발생 → 큐 소진 → passTurn → **다음 편 턴에서도 capturedBonus가 true로 잔류**. 다음 편이 다시 던질 수 있는 비정상 상태. |
| **현재 구현 (2026-05-31)** | MOVE_PIECE에서 `passTurn()` 직후 `game.capturedBonus = false`를 명시적으로 리셋. 다른 보너스 플래그(`lastResult`)와 라이프사이클 일치. |
| **2026-06-11 정밀화 (FIX-4)** | THROW_YUT의 capturedBonus 소진을 **"큐가 빈 상태에서 capturedBonus 권리로 진입한 던지기"** 1회에 한정(`enteredViaCapturedBonus = pendingResults.length === 0 && capturedBonus === true`). 윷·모 보너스 권리로 진입한 던지기(큐에 yut/mo 잔여)에서는 capturedBonus 보존. 이전에는 capturedBonus=true이면 무조건 소진되어 잡기 보너스 권리를 부당하게 잃던 엣지 케이스를 해소. |
| **영향도** | **HIGH (해소됨)** — 턴이 안 넘어가는(또는 잘못된 편이 더 던지는) 결정적 버그. |
| **사용자 영향** | 이전: 특정 시퀀스(잡기 후 큐 소진 후 추가 던지기)에서 턴 권한이 꼬임. "내 차례인데 상대가 윷을 던졌다"라는 즉각 신고 가능. 현재: 해소(+FIX-4 소진 조건 한정). |
| **상태** | **2026-05-31 해소 + 2026-06-11 소진 조건 정밀화**. 변경 시 함정 표에도 등록 (`yutnori/CLAUDE.md` 참조). 신규 회귀 `rulebook-c18-bonus-precision.spec.js`(YR-C18-001~004) + `qa-rulefix-edge.spec.js`(QA-RF4). 추적성을 위해 항목 유지. |

---

### §13-12 윷·모로 잡은 경우 중복 보너스 차단 (§6-1) [LOW, **2026-06-15 해소**]

| 영역 | 잡기 보너스 중복 (§6-1, §6-4) |
|---|---|
| **표준 룰 (출처: 한국어 위키)** | "윷이나 모를 이용하여 다른 참가자의 행마를 잡은 경우에는 중복해서 다시 던지기를 할 수 없다." — 윷·모로 잡으면 한 번만 다시 던짐. |
| **이전 구현 (버그)** | 윷·모 보너스 토큰(`lastResult`)과 잡기 보너스 플래그(`capturedBonus`)가 별도 누적되어, 잡기에 사용한 결과가 윷·모이면 두 보너스가 모두 발생(추가 던지기 2회 가능). |
| **현재 구현 (해소)** | `MOVE_PIECE`/`CHOOSE_PATH`에 `if (moveRes.captured && useResult !== 'yut' && useResult !== 'mo') game.capturedBonus = true;` 가드 추가. 윷·모로 잡아도 `capturedBonus` 미부여(중복 차단) — 윷·모 던지기 자체가 이미 추가 던지기 권리 1회를 부여하므로. 도/개/걸로 잡기는 자체 보너스가 없으므로 `capturedBonus` +1 유지. |
| **영향도** | **LOW** |
| **사용자 영향** | 윷·모로 잡았을 때 추가 던지기가 정통 룰대로 1회로 제한됨(이전 2회 가능 → 해소). 순서 무관(윷/모 던지기 먼저든 잡기 먼저든 동일). |
| **회귀 가드** | `tests/rulebook-c19-capture-bonus-no-stack.spec.js` YR-C19-001~006 (윷/모 잡기 미부여 + 도/개/걸 잡기 부여 유지 + 순서 무관성). `tests/rulebook-c8-bonus.spec.js` YR-C8-009 기댓값 갱신(중복 차단). §13-11 FIX-4 소진 로직과 무충돌. |
| **상태** | **해소** (2026-06-11 신규 등록 → 2026-06-15 해소). |

---

## 사용자 의심 후보 3건 — §13 매핑 요약

> Planner가 "사용자가 '뭔가 많이 이상하다'고 보고"의 원인으로 가능성 높게 본 후보 3건.

| 순위 | §13 항목 | 가능성 | 한 줄 설명 |
|---|---|---|---|
| **1순위** | **§13-1 모서리 강제 지름길** | ~~매우 높음~~ **2026-06-11 해소** | 외곽/지름길 선택 분기로 정통 룰 정합(FIX-2). |
| **2순위** | **§13-2 centerExitB 즉시 완주** | ~~높음~~ **2026-06-11 해소** | 23→24→25→GOAL 잔여 소진으로 정통 룰 정합(FIX-3). |
| **3순위** | **§13-4 윷가락 매핑 이력** | 중간 | 현재는 정상이나 Phase 2→2.1 정정 이력이 있어 사용자 기억과 차이 가능. 회귀 위험 상존. |

---

## 부록 — WebSocket 프로토콜 (참고)

본 룰북은 게임 규칙 중심이므로 프로토콜 상세는 `CLAUDE.md` 와 `README.md` 참조.

### 룰 관련 핵심 메시지

| 방향 | 타입 | 룰 관련 페이로드 |
|------|------|---|
| C→S | THROW_YUT | (없음) — 권위: 서버가 결과 결정 (§3) |
| S→C | YUT_RESULT | `{ result, steps, bonus, markedIndex, discarded }` |
| C→S | MOVE_PIECE | `{ pieceIndex, useResult }` — 큐에서 차감 (§5) |
| S→C | BRANCH_REQUEST | `{ pieceIndex, playerId, branchType: 'center'\|'corner' }` — 중앙/모서리 분기 대기 (§10-2/3) |
| C→S | CHOOSE_PATH | `{ pathChoice: 'top'\|'bottom'\|'outer'\|'shortcut' }` — center: top/bottom, corner: outer/shortcut. 내부 합성값 `'shortcut-top'\|'shortcut-bottom'`은 서버 전용(중첩 분기) |
| S→C | STATE | 전체 게임 상태 broadcast + `awaitingBranchType: 'center'\|'corner'\|null` (§11) |
| S→C | GAME_OVER | `{ winner }` (§11) |

---

## 변경 이력

- 2026-06-15: **§13-5 [LOW] · §13-6 [LOW] 해소** — 첫칸 빽도 워프 + 지름길B 중앙 자동 출구.
  - §13-5 — `computeNextCell` `steps === -1` 분기에 첫칸 특례: `cell 1 → cell 19`(워프), `cell 19 → cell 1`(복귀). 외곽 범용 후퇴 범위를 `2~18`로 좁힘. done=false 유지(GOAL 아님). HOME 백도 자동 폐기·cell 0 백도 정책은 미접촉. 회귀 YR-C5-001/010/011/012/013, U-56/56b/65.
  - §13-6 — 지름길B 경유 중앙(23) 정확 정착(`piece.lastPath==='shortcutB'`) 시 다음 이동 자동 centerExitB(bottom), BRANCH_REQUEST 미발송. `computeNextCell` 반환 `finalPath` 추가 + Piece `lastPath` 필드(STATE 미노출) + movePiece 저장 + MOVE_PIECE 자동 분기 + CHOOSE_PATH 방어 분기. 지름길A 자유 선택 유지, 중첩 분기(cell 5/10) 무충돌. 회귀 YR-C7-008/011/012, YR-C16-011·QA-RF2 무영향.
  - 테스트: 서버리스 회귀 327(신규 6건 YR-C5-011/012/013·YR-C7-011/012·U-56b) + 봇 스모크 7 PASS. STATE 스키마 무변경(클라이언트 무수정).
- 2026-06-15: **§13-12 [LOW] 해소** — 윷·모로 잡은 경우 중복 보너스 차단(§6-1). `server.js` MOVE_PIECE(~972)/CHOOSE_PATH(~1058) capturedBonus 부여에 `useResult !== 'yut' && useResult !== 'mo'` 가드 추가. 윷·모 던지기 자체가 이미 추가 던지기 권리를 부여하므로 그 윷·모로 잡아도 잡기 보너스 미부여(중복 차단). 도/개/걸로 잡기는 capturedBonus +1 유지. §13-11 FIX-4 소진 로직과 무충돌. 회귀 가드 YR-C19-001~006 신규(`rulebook-c19-capture-bonus-no-stack.spec.js`) + YR-C8-009 기댓값 갱신. 서버리스 회귀 321 + 봇 스모크 7 PASS.
- 2026-06-11: 룰 정합 수정 4건(FIX-1~4) + 중첩 분기 수정 반영.
  - §13-1 [HIGH] **해소** — 모서리(5/10) 외곽/지름길 선택 분기(FIX-2). `BRANCH_REQUEST branchType: 'corner'`, `CHOOSE_PATH 'outer'|'shortcut'`, 중첩 분기 2단계 모달(`shortcut-top`/`shortcut-bottom` 서버 합성).
  - §13-2 [HIGH] **해소** — centerExitB `23→24→25→GOAL` 잔여 steps 소진(FIX-3). 인덱스 24/25 활성화, 잡기/업기/백도 동작, board.js `buildCenterExitB()`.
  - §13-11 [HIGH] FIX-4 정밀화 — capturedBonus 소진을 "큐 빈 상태 capturedBonus 진입 THROW"로 한정(윷/모 보너스 진입 시 보존).
  - §13-12 [LOW, 미해소] 신규 등록 — 윷·모로 잡은 경우 중복 보너스 차단(§6-1) 미구현, 별도 발주 예정.
  - FIX-1 — 재입장 ID 중복 데드락 수정(미사용 id 배정, `usedIds` Set).
  - §2-2/§2-3/§9-2/§10-2/§10-3/§10-4/§6-4/§12 본문 갱신. 부록 WS 프로토콜 표에 branchType/awaitingBranchType/pathChoice 신규 값 반영.
  - 사용자 의심 후보 표: 1·2순위 해소 반영. §13 12건(미해소 7 + 해소 5).
  - 테스트: 서버리스 289 + QA 엣지 26 = 315 PASS, E2E 25, smoke 40. 신규 `rulebook-c15~c18` + `qa-rulefix-edge.spec.js`.
- 2026-05-31: 초안 작성. 13섹션 + 구현 노트 8건 + 사용자 의심 후보 3건 매핑.
- 2026-05-31 (갱신): 정통 룰 정합 수정 3건 반영 → §13에 신규 항목 3건 등록 (모두 해소 표기).
  - §13-9 [LOW, 해소] HOME 시작 위치 좌우 분리 → 양 팀 좌하 통일 (`public/js/ui.js`).
  - §13-10 [MED, 해소] HOME → 칸 N-1 단순화 → HOME → 칸 N 정통 매핑 (`server.js advanceOneCell`: `cell === -1` → return 1, `cell === 0` 안전망 추가). 걸(3) = 칸 3, 모(5) = 칸 5.
  - §13-11 [HIGH, 해소] MOVE_PIECE passTurn 후 `capturedBonus` 리셋 누락 → 명시 리셋 추가 (`server.js` line 828 부근).
  - §13-1, §13-2는 사용자 미보고로 그대로 유지(미해소). 사용자 의심 후보 3건 표도 갱신 없음.

---

## 참조 문서

- 사용자 문서: `../README.md`
- 컨벤션: `../CLAUDE.md`
- 현재 상태: `PROJECT.md`
- 변경 이력: `CHANGELOG.md`
- matgo 룰북 패턴(없음 — PROJECT.md/CHANGELOG.md만): `../../matgo/docs/`
- janggi 룰북 패턴: `../../janggi/docs/RULEBOOK.md`
