# 끝말잇기 배틀 기획서

> 최종 업데이트: 2026-08-10

## 프로젝트 개요

끝말잇기와 가비지 공격을 결합한 공개 HTTPS 1:1 턴제 대전 미니게임이다. 친구는 같은 공유기에서도 `https://112.155.2.238`로 접속하며 개별 포트 직접 접속은 운영하지 않는다. 두 플레이어는 하나의 단어 체인을 번갈아 이어 게이지를 채우고, 상대에게 강제 시작 글자와 HP 피해를 보낸다. 단어 판정과 매치 상태는 서버가 권위적으로 관리한다.

## 기술 스택

| 항목 | 기술 |
|---|---|
| 런타임 | Node.js 18+, ES Modules |
| 서버 | `node:http`, `ws` WebSocket |
| 클라이언트 | 바닐라 JavaScript, HTML5, CSS3 |
| 데이터 | 번들 JSON 사전, 메모리 `Set` 조회 |
| 테스트 | Playwright |

## 아키텍처

### 디렉토리 구조

```text
wordchain-battle/
├─ assets/             # 런처용 키 아트
├─ data/               # 런타임 단어 사전
├─ docs/               # 현재 상태와 변경 이력
├─ public/             # 브라우저 UI와 클라이언트 모듈
├─ scripts/            # 단어 사전 빌드 도구
├─ tests/              # Playwright 회귀 테스트와 승인 캡처
├─ ai.js               # 난수 주입 가능한 순수 보통 AI 단어 선택기
├─ bot.js              # 서버 관리형 WebSocket AI 클라이언트
├─ game.js             # 순수 게임 규칙과 상태 전이
├─ server.js           # HTTP/WebSocket 서버와 런처 어댑터
└─ words.js            # 사전 조회, 두음법칙, 가비지 후보
```

### 핵심 모듈

| 모듈 | 파일 | 역할 |
|---|---|---|
| 서버 진입점 | `server.js` | 2인 접속, JOIN 게이트, 카운트다운, 단일 턴 타이머, 타이핑 프리뷰, 서버 관리형 AI 인증·생명주기, 독립/런처 실행 |
| 게임 규칙 | `game.js` | HP·게이지·공유 체인·턴 전환·가비지·승패·리매치 상태 관리 |
| 단어 규칙 | `words.js` | 한글 명사 검증, 정방향 두음법칙, 가비지 음절 후보 생성 |
| 클라이언트 제어 | `public/js/main.js` | 서버 이벤트를 UI 상태 전이와 입력 동작에 연결 |
| 화면 렌더링 | `public/js/ui.js` | HP·게이지·체인·타이머·알림·결과 화면 렌더링 |
| 네트워크 | `public/js/network.js` | WebSocket 연결과 이벤트 디스패치 |
| 입력 검증 | `public/js/input.js` | 한글 입력 확인과 거절 사유 문구 변환 |
| 보통 AI 선택기 | `ai.js` | 사전 시작 글자 인덱스, forced·두음법칙·공용 중복·게이지·막다른 체인 평가 |
| AI 클라이언트 | `bot.js` | 1.2~2.0초 지연 제출, 일회용 자격 증명, stale 예약 차단, 거절 재시도와 자동 리매치 |

## 기능 목록

| 기능 | 설명 | 상태 |
|---|---|---|
| 2인 실시간 매치 | 두 플레이어가 모두 JOIN한 뒤 카운트다운을 시작한다. | 완료 |
| 공유 끝말잇기 체인 | 두 플레이어가 하나의 체인을 교대로 이어가며 매치 전체에서 단어 중복을 금지한다. | 완료 |
| 서버 권위 단어 판정 | 번들 사전과 정방향 두음법칙으로 제출 단어를 검증한다. | 완료 |
| 가비지 공격 | 제출 시 남은 시간마다 5%씩 게이지를 채우고, 충전 시 상대 HP와 시작 글자에 불이익을 준다. | 완료 |
| 무작위 시작 글자 | 매치마다 후속 단어가 충분한 후보에서 첫 시작 글자를 무작위 지정한다. | 완료 |
| 제한 시간과 승패 | 현재 턴의 10초 타이머 만료 시 해당 플레이어만 5 HP 피해 후 체인 조건을 유지한 채 상대에게 턴을 넘긴다. | 완료 |
| 상대 입력 프리뷰 | 상대 턴에 단어 내용은 숨기고 현재 입력 중인 글자 수만 실시간 표시한다. | 완료 |
| 리매치 | 종료 후 양쪽이 동의하면 매치 상태를 초기화해 다시 시작한다. | 완료 |
| 반응형 대전 UI | 360×640 모바일과 800×640 데스크톱에서 체인 강조와 비겹침 가비지 알림을 제공한다. | 완료 |
| 런처 통합 | `createApp()`과 `/wordchain-battle/` 라우트로 미니게임 런처에 연결된다. | 완료 |
| 1인 보통 AI 대전 | 런처의 `AI로 채우기`에서 사람 1명과 서버 관리형 `AI (보통)` 1명을 매칭한다. AI도 기존 `WORD_SUBMIT`·`REMATCH` 경로만 사용한다. | 완료 |
| AI 세션 무결성 | 서버가 생성한 현재 자식 프로세스만 일회용 자격 증명으로 봇 슬롯에 접속할 수 있다. | 완료 |
| 독립 실행 | 포트 옵션을 받아 단독 서버로 실행할 수 있다. | 완료 |

## 알려진 제약사항

- AI 난이도는 보통 한 종류이며 학습형 AI·전적·AI 전용 보상은 제공하지 않는다.
- 단어 사전은 서버 시작 시 메모리에 적재되며 실행 중 외부 사전과 동기화하지 않는다.
- 게임은 단일 2인 방을 관리하며 다중 방 매칭은 지원하지 않는다.
- 사용자 노출 UI는 한국어 단일 언어다.

## 서버 안전성 및 이벤트 의미

- 카운트다운은 timeout 취소와 세대 번호, 게임 객체 및 접속 플레이어 identity 검증을 함께 사용한다. 재접속이나 리매치로 교체된 게임에는 이전 예약 콜백이 영향을 주지 않는다.
- 정적 파일은 URL을 한 번 디코딩한 뒤 `..`, 역슬래시, NUL을 거부하고 `path.relative()`로 `public/` 또는 `assets/` 루트 내부인지 확인한다.
- `STATE.turn`은 현재 입력 가능한 플레이어이며 `STATE.chain`은 양쪽이 공유하는 마지막 단어·시작 글자·가비지 강제 조건을 담는다.
- `WORD_ACCEPTED.wasGarbage`는 제출자가 직전에 받은 강제 글자에 대응한 단어임을 뜻한다. `garbagedOpponent`는 해당 제출로 상대에게 새 가비지를 발사했는지 뜻하므로 두 필드는 독립적이다.

## 향후 계획

- 다중 방 매칭, AI 난이도 선택, 전적·보상 시스템은 현재 범위 밖이다.
